version: 0.2

phases: 
  install: 
    commands: 
      - "echo installing dependencies"
      - npm ci

  build: 
    commands: 
      - "echo running build"
      - node app.js 

  post_build: 
    commands: 
      - "echo build completed successfully"

artifacts: 
  files: 
    - '**/*'


